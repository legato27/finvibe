import { JournalPage } from "@/components/desk/JournalPage";

// Desk → Journal: the whole trading journal, every family, with its activity.
export default function Page() {
  return <JournalPage />;
}
